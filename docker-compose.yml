version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - app_data:/data        # banco SQLite + fotos persistem aqui
    env_file:
      - .env
    restart: unless-stopped

volumes:
  app_data:
