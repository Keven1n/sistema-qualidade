version: "3.9"

volumes:
  app_data:
services:
  app:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - app_data:/data
      - ./app:/app/app          # mapeia a pasta local direto no container
      - ./templates:/app/templates
    env_file:
      - .env
    restart: unless-stopped
